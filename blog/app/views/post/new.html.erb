<h1>Novo Post </h1>

<%= form_for @post do |f| %>
 <%= f.label :nome %>:
  <%= f.text_field :nome %><br />

  <%= f.label :conteudo %>:
  <%= f.text_area :conteudo %><br />

 <!--<%= f.collection_select(:categories, Category.all,:id, :name, {:prompt=>true}) %> -->

<% for x in Category.find(:all) %>
<div>
  <%= check_box_tag "categories", x.id, @post.categories.include?(x) %>
  <%= x.name %>
</div>
<% end %>
  
  <%= f.submit%>
  <% end %>
